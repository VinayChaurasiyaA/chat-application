package com.involveinnovation.Chatserver.controller.model;
// Note that this is an enum class :
// what is enum : An enum is a special "class" that represents a group of constants (unchangeable variables, like final variables).
//To create an enum, use the enum keyword (instead of class or interface), and separate the constants with a comma
public enum Status {
    JOIN,MESSAGE,LEAVE
}
